import Config from 'react-native-config';

// Environment variables module
console.log('[ENV] Config object loaded:', Config ? 'Yes' : 'No');
console.log('[ENV] All Config keys:', Object.keys(Config));

// Export any environment variables you need here
// (OpenAI references removed)
