name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  NODE_VERSION: '20'
  JAVA_VERSION: '17'

jobs:
  # Code Quality & Testing
  quality-check:
    name: Code Quality & Testing
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
        
      - name: Run ESLint
        run: npm run lint -- --max-warnings=0
        
      - name: Run tests
        run: npm test -- --coverage=false --silent
        
      - name: Check TypeScript compilation
        run: npx tsc --noEmit

  # Security Audit
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
        
      - name: Run security audit
        run: npm audit --audit-level=high || true
        
      - name: Check for hardcoded secrets
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          extra_args: --only-verified

  # Sentry Release Tracking
  sentry-release:
    name: Sentry Release
    runs-on: ubuntu-latest
    needs: [quality-check, security-audit]
    if: github.ref == 'refs/heads/main'
    env:
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
      SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Sentry configuration
        id: sentry-config
        shell: bash
        run: |
          if [ -z "$SENTRY_AUTH_TOKEN" ] || [ -z "$SENTRY_ORG" ] || [ -z "$SENTRY_PROJECT" ]; then
            echo "::warning::Sentry release skipped because one or more Sentry secrets are not configured."
            echo "configured=false" >> "$GITHUB_OUTPUT"
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create Sentry release
        if: steps.sentry-config.outputs.configured == 'true'
        uses: getsentry/action-release@v1
        env:
          SENTRY_AUTH_TOKEN: ${{ env.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ env.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ env.SENTRY_PROJECT }}
        with:
          ignore_empty: true
          environment: production
          version: ${{ github.sha }}
          url_prefix: 'app://'
          dist: '1'

      - name: Skip Sentry release
        if: steps.sentry-config.outputs.configured != 'true'
        run: echo "Sentry release skipped; CI can continue without release tracking."

  # Android Build
  android-build:
    name: Android Build
    runs-on: ubuntu-latest
    needs: [quality-check, security-audit, sentry-release]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        
      - name: Install dependencies
        run: npm ci
        
      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
            
      - name: Build Android APK
        run: |
          cd android
          ./gradlew assembleDebug --no-daemon
          
      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

  # iOS Build (only on main branch)
  ios-build:
    name: iOS Build
    runs-on: macos-latest
    needs: [quality-check, security-audit, sentry-release]
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.0'
          bundler-cache: true
          working-directory: ios
          
      - name: Install CocoaPods
        run: |
          cd ios
          bundle exec pod install
          
      - name: Build iOS
        run: |
          cd ios
          xcodebuild -workspace siFia.xcworkspace \
                     -scheme siFia \
                     -configuration Debug \
                     -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' \
                     -derivedDataPath DerivedData \
                     build
                     
      - name: Upload iOS Build
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: ios/DerivedData/Build/Products/Debug-iphonesimulator/siFia.app
          retention-days: 30

  # Deploy to TestFlight (only on main branch)
  deploy-testflight:
    name: Deploy to TestFlight
    runs-on: macos-latest
    needs: [ios-build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: staging
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.0'
          bundler-cache: true
          working-directory: ios
          
      - name: Install CocoaPods
        run: |
          cd ios
          bundle exec pod install
          
      - name: Build for TestFlight
        run: |
          cd ios
          xcodebuild -workspace siFia.xcworkspace \
                     -scheme siFia \
                     -configuration Release \
                     -destination generic/platform=iOS \
                     -archivePath DerivedData/siFia.xcarchive \
                     archive
                     
      - name: Export IPA
        run: |
          cd ios
          xcodebuild -exportArchive \
                     -archivePath DerivedData/siFia.xcarchive \
                     -exportPath DerivedData \
                     -exportOptionsPlist ExportOptions.plist
                     
      - name: Upload to TestFlight
        run: |
          xcrun altool --upload-app \
                       --type ios \
                       --file ios/DerivedData/siFia.ipa \
                       --username ${{ secrets.APPLE_ID }} \
                       --password ${{ secrets.APPLE_APP_PASSWORD }}
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}

  # Performance Analysis
  performance-check:
    name: Performance Analysis
    runs-on: ubuntu-latest
    needs: [quality-check]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Check Android bundle size
        run: |
          ulimit -n 4096
          mkdir -p /tmp/rn-assets
          npx react-native bundle \
            --platform android \
            --dev false \
            --entry-file index.js \
            --bundle-output /tmp/bundle.js \
            --assets-dest /tmp/rn-assets \
            --reset-cache

          BUNDLE_SIZE=$(wc -c < /tmp/bundle.js | tr -d ' ')
          MAX_BUNDLE_SIZE=10485760

          echo "## Performance Metrics" >> $GITHUB_STEP_SUMMARY
          echo "- Bundle size: ${BUNDLE_SIZE} bytes" >> $GITHUB_STEP_SUMMARY
          echo "- Target: < ${MAX_BUNDLE_SIZE} bytes" >> $GITHUB_STEP_SUMMARY
          
          if [ "$BUNDLE_SIZE" -gt "$MAX_BUNDLE_SIZE" ]; then
            echo "- Status: FAIL" >> $GITHUB_STEP_SUMMARY
            echo "Bundle size too large: ${BUNDLE_SIZE} bytes"
            exit 1
          fi

          echo "- Status: PASS" >> $GITHUB_STEP_SUMMARY

  # Notification
  notify:
    name: Notify Results
    runs-on: ubuntu-latest
    needs: [quality-check, security-audit]
    if: always()
    
    steps:
      - name: Notify Success
        if: ${{ needs.quality-check.result == 'success' && needs.security-audit.result == 'success' }}
        run: |
          echo "✅ CI/CD Pipeline Completed Successfully!"
          echo "🧪 All tests passed"
          echo "🔒 Security audit passed"
          echo "📋 Code quality checks passed"
          
      - name: Notify Failure
        if: ${{ needs.quality-check.result == 'failure' || needs.security-audit.result == 'failure' }}
        run: |
          echo "❌ CI/CD Pipeline Failed!"
          echo "Check the job logs for details"
          exit 1
