name: Daily Notifications - Morning Batch

on:
  schedule:
    # Run at 11:00 PM UTC (7:00 AM Philippines) - for same-day notifications
    - cron: '0 23 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  generate-morning-notifications:
    name: Generate Morning Notification Batch
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Generate daily notifications
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🌅 Generating morning notification batch (7 AM Philippines)..."
          
          # Call the Supabase edge function for daily batch
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/generate-daily-notifications" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}')
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          
          echo "Response: $BODY"
          echo "HTTP Code: $HTTP_CODE"
          
          if [ "$HTTP_CODE" -ne 200 ]; then
            echo "❌ Morning notification generation failed with code $HTTP_CODE"
            exit 1
          fi
          
          echo "✅ Morning notification batch completed successfully"
          
      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Morning Notification Batch Failed',
              body: 'The morning notification generation job (7 AM Philippines) failed. Please check the logs and investigate immediately.\n\nThis affects user engagement and retention.',
              labels: ['bug', 'urgent', 'notifications']
            })
